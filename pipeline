pipeline {
    agent any
    environment {
        TEST_ENV = 'dev'
    }
    stages {
        stage('Build') {
            steps {
                sh 'LaunchInstance.sh'
            }
        }
    }
}
