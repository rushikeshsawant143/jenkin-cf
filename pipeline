pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                    echo "This is step script"
                    ls -ltr
                    pwd
                '''
                sh './LaunchInstance.sh'
            }
        }
    }
}
