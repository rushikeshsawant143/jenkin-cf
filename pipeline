pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                    echo "This is step script"
                    ls -ltr
                    ll
                    chmod +x .
                '''
                sh 'LaunchInstance.sh'
            }
        }
    }
}
