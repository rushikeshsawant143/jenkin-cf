pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'LaunchInstance.sh'
                sh '''
                    echo "This is step script"
                    ls -ltr
                '''
            }
        }
    }
}
