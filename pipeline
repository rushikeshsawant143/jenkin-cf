pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                    echo "This is step script"
                    ls -ltr
                    ll
                    chmod +x LaunchInstance.sh
                    chmod +x EC2_instance.yml
                    chmod +x pipeline
                    ll
                '''
                sh 'LaunchInstance.sh'
            }
        }
    }
}
